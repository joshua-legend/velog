import React from 'react';
import styles from './cardpreview.module.css'

const Cardpreview = props => {
    return (
        <section className={styles.container}>
            <header className={styles.title}>Card Preview</header>
        </section>
    );
};

export default Cardpreview;
