import React from 'react';
import styles from './cardmaker.module.css'

const Cardmaker = props => {
    return (
        <section className={styles.container}>
            <header className={styles.title}>Card Maker</header>
        </section>
    );
};
export default Cardmaker;
